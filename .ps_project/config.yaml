version: 2
workflows:
  single-simple:
    steps:
      - name: openvino-pipeline
        command: experiment.run_single_node
        paramsFile: .ps_project/wolfpass.yaml
        checks: #[optional]
          custom:MSE:
             target: "0.8..1"
             aggregate: /accuracy
          defaults: #[optional]
              precision: 3
    triggers:
        branches:
          only: .*
        tags:
          only:
            - v.*
            - latest
